/** @type {import('tailwindcss').Config} */
module.exports = {
    // Specify the paths to all of the template files in your project
    content: [],
    
    theme: {
      // Extend the default theme to add custom styles
      extend: {},
    },
    
    // Add any plugins that you're using here
    plugins: [],
  }
  